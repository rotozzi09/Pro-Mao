# Trimmed dependencies for the Base44 preview — only packages actually imported
# by server.py and its tests. The full requirements.txt pulls private/internal
# wheels (emergentintegrations, litellm) that are not needed to run the app here.
python-dotenv==1.2.3
fastapi==0.110.1
starlette==0.37.2
motor==3.3.1
pydantic==2.13.5
email-validator==2.3.0
bcrypt==4.1.3
PyJWT==2.13.0
resend==2.42.0
pymongo==4.6.3
requests==2.34.2
uvicorn==0.25.0
httpx==0.28.1
pytest==9.1.1
pytest-xdist==3.8.0
